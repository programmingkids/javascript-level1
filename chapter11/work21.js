var base = 8
var height = 4;

// ここに関数「getTriangleArea」を定義します
function getTriangleArea(base, height) {
    return (base * height) / 2;
}

var result = getTriangleArea( base, height );
console.log( result ); // 16
