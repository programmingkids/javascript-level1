var num1 = 3;
var num2 = 5;
var num3 = 6;

// ここに関数「allAll」を定義します
function addAll(num1, num2, num3) {
    return num1 + num2 + num3;
}

var result = addAll( num1, num2, num3 );
console.log( result ); // 14
